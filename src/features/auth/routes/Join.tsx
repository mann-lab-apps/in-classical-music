import { JoinForm } from '../components';
import { Layout } from '../components/Layout';

export const Join = () => {
  return (
    <Layout heading="회원가입">
      <JoinForm />
    </Layout>
  );
};
